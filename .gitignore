node_modules/
coverage/
dist/
.expo/
*.log
.DS_Store

# WASM is copied from node_modules by scripts/prep-web.mjs — do not commit.
# The hand_landmarker.task model IS committed (below) so gestures work offline /
# behind a firewall with no runtime download.
public/mediapipe/wasm/

# native build output (3단계 이후 생성)
android/
ios/
!android/.gitkeep
!ios/.gitkeep
